问题:由于当前页面中,video组件过多,每个video都有自己src属性,会一起请求数据
官方:页面中别超过3个video组件,可以使用image替代video

需求:使用image组件替换所有的video组件,减少视频资源的请求
问题:image标签无法播放视频

需求:当用户点击了某个image组件时,播放对应的视频
拆解:
	1.当用户点击了某个image组件时
		事件名:tap||touchstart
	2.播放对应的视频
		1)需要显示对应的视频组件(因为当前页面上只有image组件)
			通过data控制image组件和video组件之间的切换
		2)如何知道被点击的图片和哪个video是对应关系
			给图片和视频都添加一个共同的标识
		3)自动播放视频
			详情参考上一个思路
